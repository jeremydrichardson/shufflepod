version: '3.8'

services:
  shufflepod:
    build: .
    ports:
      - "3000:3000"
    environment:
      - BASE_URL=http://localhost:3000
      - PORT=3000
      - CACHE_TTL_SECONDS=3600
      - MAX_FEED_SIZE_MB=50
      - REQUEST_TIMEOUT_MS=30000
    restart: unless-stopped
